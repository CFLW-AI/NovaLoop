{
  "name": "auto-research",
  "private": true,
  "scripts": {
    "build": "cd docs && npm install && npm run build",
    "dev": "cd docs && npm run dev"
  },
  "dependencies": {
    "next": "16.1.6"
  }
}
